{
  "name": "@crema/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared zod schemas and TS types consumed by backend/ and frontend/. Single source of truth — import from here, never redefine.",
  "type": "module",
  "exports": {
    ".": "./schemas/index.ts",
    "./schemas": "./schemas/index.ts",
    "./schemas/*": "./schemas/*.ts",
    "./types": "./types/index.ts",
    "./types/*": "./types/*.ts"
  },
  "peerDependencies": {
    "@hono/zod-openapi": "^1.4.0",
    "zod": "^4.4.3"
  }
}
